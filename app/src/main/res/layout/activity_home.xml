<?xml version="1.0" encoding="utf-8"?>
<!-- GANDA PLUS — Ultra-minimal AMOLED MD3 Home Screen -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/md3_surface"
    android:fitsSystemWindows="true">

    <!-- Top Bar -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/md3_surface"
        app:elevation="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:gravity="center_vertical"
            android:paddingStart="20dp"
            android:paddingEnd="12dp"
            android:orientation="horizontal">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/app_name"
                    android:textColor="@color/md3_on_surface"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    android:letterSpacing="0.02" />

                <TextView
                    android:id="@+id/home_app_count"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text=""
                    android:textColor="@color/md3_on_surface_variant"
                    android:textSize="12sp" />
            </LinearLayout>

        </LinearLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- Clone list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/home_launcher"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="100dp"
        android:scrollbars="none"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <!-- Loading indicator (while fetching list) -->
    <LinearLayout
        android:id="@+id/pb_loading_app"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:indeterminate="true"
            app:indicatorColor="@color/md3_primary"
            app:trackColor="@color/md3_surface_container_high" />
    </LinearLayout>

    <!-- FAB Container -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:orientation="vertical"
        android:gravity="end">

        <!-- FAB: Location Spoof -->
        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/fab_location"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:text="  Fake Location"
            android:textColor="@color/md3_on_surface"
            android:textSize="13sp"
            android:textStyle="bold"
            app:backgroundTint="@color/md3_surface_container_high"
            app:icon="@drawable/ic_location"
            app:iconTint="@color/md3_primary"
            app:elevation="4dp"
            app:shapeAppearanceOverlay="@style/ShapeLarge" />

        <!-- FAB: Add / Clone App -->
        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/fab_add_app"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="  Clone App"
            android:textColor="@color/md3_on_primary"
            android:textSize="15sp"
            android:textStyle="bold"
            app:backgroundTint="@color/md3_primary"
            app:icon="@drawable/ic_add"
            app:iconTint="@color/md3_on_primary"
            app:elevation="4dp"
            app:shapeAppearanceOverlay="@style/ShapeLarge" />
    </LinearLayout>

    <!-- Drag bottom bar (drag-to-delete) — hidden unless dragging -->
    <LinearLayout
        android:id="@+id/bottom_area"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_gravity="bottom"
        android:background="@color/md3_surface_container_high"
        android:orientation="horizontal"
        android:visibility="gone"
        android:baselineAligned="false"
        android:weightSum="1"
        android:elevation="8dp">

        <LinearLayout
            android:id="@+id/create_shortcut_area"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.5"
            android:gravity="center"
            android:orientation="vertical">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_shortcut"
                android:tint="@color/md3_primary" />

            <TextView
                android:id="@+id/enter_app_setting_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Settings"
                android:textColor="@color/md3_on_surface_variant"
                android:textSize="11sp" />
        </LinearLayout>

        <View
            android:layout_width="1dp"
            android:layout_height="match_parent"
            android:background="@color/md3_outline_variant"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="12dp" />

        <LinearLayout
            android:id="@+id/delete_app_area"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.5"
            android:gravity="center"
            android:orientation="vertical">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_crash"
                android:tint="@color/md3_error" />

            <TextView
                android:id="@+id/delete_app_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Remove"
                android:textColor="@color/md3_on_surface_variant"
                android:textSize="11sp" />
        </LinearLayout>
    </LinearLayout>

    <!-- Modal Loading Overlay - MD3 Styled -->
    <FrameLayout
        android:id="@+id/modal_loading_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#E6000000"
        android:clickable="true"
        android:elevation="100dp"
        android:focusable="true"
        android:visibility="gone">

        <androidx.cardview.widget.CardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            app:cardBackgroundColor="#131821"
            app:cardCornerRadius="24dp"
            app:cardElevation="16dp"
            android:outlineAmbientShadowColor="#004D64"
            android:outlineSpotShadowColor="#334FC3F7">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="vertical"
                android:padding="32dp">

                <com.google.android.material.progressindicator.CircularProgressIndicator
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:indeterminate="true"
                    app:indicatorColor="#4FC3F7"
                    app:trackColor="#1A4FC3F7"
                    app:indicatorSize="48dp"
                    app:trackThickness="4dp" />

                <TextView
                    android:id="@+id/modal_loading_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="20dp"
                    android:text="Launching…"
                    android:textColor="#E2E2E9"
                    android:textSize="14sp"
                    android:textStyle="bold" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>
    </FrameLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>